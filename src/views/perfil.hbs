<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Bienvenido {{user.fullname}}</h3> {{!-- el objeto user esta alojado localmente con express.locals --}}
                    <h4>{{user.username}}</h4>
                    <a href="/cuentas" class="btn btn-primary m-4">
                        Cuentas
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>