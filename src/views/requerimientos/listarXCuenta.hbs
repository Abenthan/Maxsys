<div class="container">
    <div class="row">
        <div class="col">
            <table class="table table-striped table-bordered table-hover">
                <thead>
                    <tr>
                        <th>Usuario</th>
                        <th>Asunto</th>
                        <th>Estado</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each requerimientos}}
                    <tr>
                        <td>{{username}}</td>
                        <td>{{asunto}}</td>
                        <td>{{estado}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>

        </div>
    </div>
</div>